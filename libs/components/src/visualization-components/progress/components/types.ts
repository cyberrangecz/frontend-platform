export enum TraineeViewEnum {
    Name = 'name',
    Avatar = 'avatar',
    Both = 'both',
}

export class DisplayView {
    id: number;
    name: string;
}

export enum ViewEnum {
    Progress = 'progress',
    Overview = 'overview',
}
